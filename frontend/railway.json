{
  "$schema": "https://railway.com/railway.schema.json",
  "build": {
    "builder": "DOCKERFILE",
    "dockerfilePath": "Dockerfile",
    "buildArgs": {
      "VITE_API_URL": "https://personelpaneli-production.up.railway.app/api"
    }
  },
  "deploy": {
    "startCommand": "serve -s dist -l 3000"
  }
}
